name: double_tap_player_view_plus_example
description: Demonstrates how to use the double_tap_player_view_plus plugin.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.6.2
  flutter: ^3.27.4

dependencies:
  flutter:
    sdk: flutter
  double_tap_player_view_plus:
    path: ../
  flutter_hooks:
  hooks_riverpod:
  video_player: ^2.1.12

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true